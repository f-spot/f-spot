AC_DEFUN([FSPOT_CHECK_GNOME_SHARP],
[
        GNOMESHARP_REQUIRED=2.8

        PKG_CHECK_MODULES(GCONFSHARP,
                gconf-sharp-2.0 >= $GNOMESHARP_REQUIRED)
        AC_SUBST(GCONFSHARP_LIBS)
])
