#!/bin/sh

if [ -e ./f-spot.exe ] && [ -e ./Makefile.am ] && [ -e ./PhotoStore.cs ]; then
    echo "*** Running uninstalled f-spot ***"
    EXE_TO_RUN="./f-spot.exe"
else
    EXE_TO_RUN="@prefix@/lib/f-spot/f-spot.exe"
fi

mono --debug $EXE_TO_RUN "$@"
